# Local development — point at the real API Gateway endpoint
# Replace with a local wscat tunnel or sam local start-api if you want fully offline dev
VITE_WS_ENDPOINT=wss://hyupn8hi52.execute-api.us-west-2.amazonaws.com/prod
